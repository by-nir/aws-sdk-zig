pub const SerialType = enum {
    boolean,
    byte,
    short,
    integer,
    long,
    float,
    double,
    blob,
    string,
    int_enum,
    str_enum,
    trt_enum,
    big_integer,
    big_decimal,
    timestamp,
    list,
    set,
    map,
    document,
    structure,
    tagged_union,
};
